{
  "name": "ghazdo-widget",
  "version": "1.0.0",
  "description": "GitHub Advanced Security for Azure DevOps Widget",
  "private": true,
  "devDependencies": {
    "tfx-cli": "^0.23.1"
  },
  "scripts": {
    "install-tfx": "npm install",
    "test": "npm --prefix widgets test && npm --prefix dependencyReviewTask test",
    "coverage": "npm --prefix widgets run coverage && npm --prefix dependencyReviewTask run coverage"
  }
}
